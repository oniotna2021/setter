import React from "react";

const LockedCell = () => {
  return (
    <td
      style={{
        cursor: "auto",
      }}
      className="promotionLockedCell"
    ></td>
  );
};

export default LockedCell;
